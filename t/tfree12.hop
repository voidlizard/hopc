
($ ccall random :int $)
($ ccall display :int :unit $)



(define (fx) (let ((y 22)) (let ((z 33) (m 77)) (begin ((lambda () (display 3))) (display x) (fy)))))

(define (fy) (display x))

(define (FX1) (let ((y 22)) (let ((z 33) (m 77)) (begin (display x) (FY1) ((lambda () (display y)))  ))))

(define (FY1) (display 3) ((lambda () (display 600))) )

(fx)
(fy)
(FX1)
(FY1)

(define x (random))

