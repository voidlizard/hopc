
($ foreign "display" display ((:string) :unit) $) 

(define (mutapaka) (display "mutapaka"))

(define (qqq a b c) 
    (display a)
    (display b)
    (display c)
	22
)

(define (boo x) (qqq "A" "B" x))

(boo "C")

