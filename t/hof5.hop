($ foreign "display" display ((:string) :unit) $)
($ foreign "newline" newline ((:unit) :unit)   $)

(define (q s) (display s))

($ typeof f1 ((((:string) :unit) ((:string) :unit)) :unit) $)

(define (f1 fput fput2)
    (q "Q")
    (newline)
    (fput "PIU")
    (newline)
    (q "Q")
    (newline)
    (fput2 "PYSH PYSH")
    (newline)
)

(f1 q display)


