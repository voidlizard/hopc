
($ ccall random :int $)
($ ccall + :int :int :int $)
($ ccall - :int :int :int $)

(define (main)
    (let ((x 55)
          (fx (lambda (n) (fy (+ x n))))
          (fy (lambda (n) (fx (- n x))))
         )
         (fx 1)
    )
)

(main)

