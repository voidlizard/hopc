
($ foreign "eq" eq? ((:int :int) :bool) $)
($ foreign "display" display ((:string) :unit) $)

(define x 10)

(if (eq? x 1)
    (display "1")
    (if (eq? x 2)
		(display "2")
		(if (eq? x 3)
		    (display "3")
			(display "other")
		)
	)
)

(display "END")

