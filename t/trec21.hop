
($ ccall random :int $)
($ ccall + :int :int :int $)
($ ccall - :int :int :int $)

(define (main)
    (let ((x (random))
          (fx (lambda (n) (fy (+ x n))))
          (fy (lambda (n) (fx (- x n))))
         )
         (fx 1)
    )
)

(main)

